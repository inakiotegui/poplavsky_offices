---
to: <%= dest %>/<%= name %>/<%= name %>.jsx
---
<% console.log(`Destination: ${dest}/${name}/${name}.jsx`); %>
import React from 'react';
import './<%= name %>.css';

export default function <%= name %> () {
  return (
    <div className="<%= name %>">
      <h1><%= name %> Component</h1>
    </div>
  );
};